# MongoDB Atlas connection string (Compass: right-click cluster → "Copy Connection String")
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.kulbyec.mongodb.net/?retryWrites=true&w=majority

# Database name to use for this project (kept separate from your other clients' databases)
MONGODB_DB=nexacore
